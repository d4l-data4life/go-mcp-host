# Local secrets example
# DO NOT commit this file with real credentials
# Copy to secrets.yaml and update with your values

# Database connection details
DBName: mcphost
Hostname: postgres-go-mcp-host.default.svc.cluster.local
Port: 5432
username: mcphost
password: changeme
SSLMode: disable

# JWT secret (for local development only)
GO_SVC_TEMPLATE_SECRET: local-dev-secret-change-in-production

