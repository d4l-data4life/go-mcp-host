# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Compiled binaries
go-mcp-host
api
main

# Build artifacts
deploy/manifests
vendor/
deploy/helm-chart/Chart.yaml

# Build info persisted in files
BRANCH
COMMIT
VERSION

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Environment and config files (user-specific)
.env
.docker.env
config.yaml

# User-specific deployment configurations
# Keep example configurations, but ignore actual deployment values
deploy/local/
deploy/dev/
deploy/staging/
deploy/prod/
!deploy/examples/

# Secrets (double-check these are never committed!)
deploy/**/secrets.yaml
!deploy/**/secrets.example.yaml
*.pem
*.key
test-keys/

# Temporary and backup files
*.tmp
*.bak
*.log

# Test coverage reports
coverage.out
coverage.html

# Go workspace file
go.work
go.work.sum
